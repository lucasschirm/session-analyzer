{
  "name": "@lucasschirm/sal-transformer-registry",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "license": "ISC",
  "sideEffects": false,
  "description": "Default TransformerRegistry composition root: wires every transformer plugin package (claude-transformer today, future harness plugins) into a single registry for production and test consumers.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/lucasschirm/session-analyzer.git",
    "directory": "packages/transformers/registry"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc --noEmit -p tsconfig.tests.json",
    "lint": "biome check src tests",
    "preverify": "pnpm run build",
    "verify": "pnpm run typecheck && pnpm run lint && pnpm run test:coverage",
    "version:patch": "exit 0",
    "version:publish": "exit 0"
  },
  "dependencies": {
    "@lucasschirm/sal-claude-transformer": "workspace:*",
    "@lucasschirm/sal-devin-transformer": "workspace:*",
    "@lucasschirm/sal-transformer-shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@vitest/coverage-v8": "^4.1.10",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
