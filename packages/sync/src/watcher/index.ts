/**
 * watcher/ module barrel — re-exports the contract surface from `./contract.js`.
 *
 * The transcript watcher core implementation is added by TSK0011 and must be
 * exported from this barrel.
 */
export * from './contract.js';
