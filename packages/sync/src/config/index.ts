/**
 * config/ module barrel — re-exports the `config.js` implementation.
 *
 * The contract surface is re-exported by the package index from
 * `@lucasschirm/sal-sync-core`.
 */

export * from './config.js';
