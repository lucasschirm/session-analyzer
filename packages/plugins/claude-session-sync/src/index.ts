/**
 * Claude Code plugin adapter for @lucasschirm/sal-sync.
 *
 * This package is the thin Claude Code integration layer that translates
 * Claude Code hook input into the shared sync engine's HarnessSession model
 * and invokes the sal-sync CLI.
 *
 * Implementation is added by TSK0010.
 */
export {};
