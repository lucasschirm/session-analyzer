---
description: Same two globs as t7-rule-basic.md, authored as a "paths:" YAML list instead of a "globs:" comma scalar.
paths:
  - "src/**/*.ts"
  - "src/**/*.tsx"
---

# TypeScript Conventions (paths form)

Equivalent glob scoping, different frontmatter key/shape.
