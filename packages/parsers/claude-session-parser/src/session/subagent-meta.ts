import type { SubagentMeta } from '../types/config.js';

// TODO(T8): implement — see spec §3 (SubagentMeta) + §4 (parseSubagentMeta)
export function parseSubagentMeta(content: string): SubagentMeta {
  void content;
  return { parseErrors: [] };
}
